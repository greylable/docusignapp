<div class="row">
  <div class="col s8 offset-s2">
    <div class="card">
      <div class="card-content">
        <span class="card-title">List an envelope to void</span>
        <%= simple_form_for @voidenvelope do |f| %>
          <%= f.input :envelope_id%>
          <%= f.input :name%>
          <%= f.input :void_reason%>
          <div class="card-action">
            <%= link_to 'back', :back %>
            <%= f.button :submit, class: 'right' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>